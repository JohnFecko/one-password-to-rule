{
  "name": "@optr/site",
  "private": true,
  "version": "0.0.1",
  "description": "optr-site",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build --prerender",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "^4.7.4",
    "@optr/components": "^0.0.6",
    "github-buttons": "^2.2.10"
    
  },
  "devDependencies": {
    "@stencil/core": "^1.2.4",
    "@stencil/sass": "^1.0.1"
  }
}
